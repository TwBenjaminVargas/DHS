int suma(int a, int b) {
    return a + b;
}

float multiplicar(int a, int b) {
    return a * b;
}

int main() {
    int x = 10, y = 5, resultado1,resultado2;
    float resultadoMul;

	resultado1 = x + y * x;
	resultado2 = x + y * x;

    resultado1 = suma(x, y);

    resultadoMul = multiplicar(x, y);


    if (x > 0)
        x=1;
    else
		x = 2;

    int z = 0;
    resultadoMul = multiplicar(x, z);

    return 0;
}
